import styles from "./OpenAccountButton.module.scss";
const OpenAccountButton = () => {
  return (
    <>
      <button className={styles.openAccount}>Open Account</button>
    </>
  );
};
export default OpenAccountButton;
